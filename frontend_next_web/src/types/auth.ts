export interface daftar{
    nama_lengkap: string;
    no_pegawai: number;
    email: string;
    no_telp: string;
    password: string;
    status: string;
}

export interface login{
    email: string;
    password: string;
}